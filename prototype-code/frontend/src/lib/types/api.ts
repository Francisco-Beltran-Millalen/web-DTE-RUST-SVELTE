/**
 * Shared TypeScript interfaces for API request/response shapes.
 * Added as use cases are implemented in Stage 4-2.
 *
 * Example (added per use case):
 *   export interface RegisterRequest { name: string; email: string; password: string; }
 *   export interface AuthResponse { access_token: string; user: UserDto; }
 */

// Placeholder — populated in Stage 4-2 per use case
export type TODO = never;
